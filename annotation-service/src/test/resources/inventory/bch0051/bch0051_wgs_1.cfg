{
    "aliases": {
        "CASE,PLATFORM,PROJECT": "split('${NAME}', '_')"
    },
    //Test
    "name": "${NAME}",
    "case": "${CASE}",
    "platform": "${PLATFORM}",
    "project": "${PROJECT}",
    "config": "${DIR}/config.json",
    "fam": "${DIR}/${CASE}.fam",
    "patient-ids": "${DIR}/${CASE}.csv",
    "vcf": "${DIR}/${CASE}.vcf",
    "vep-json": "${DIR}/${CASE}_vep.json",
    "anno-log": "${DIR}/anno.log",
    //"anno-log": "${DIR}/anno.${TS}.log",
    "a-json": "${DIR}/${CASE}_anfisa.json.gz",
    "docs": []    
}

