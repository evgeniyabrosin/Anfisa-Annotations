{
    "aliases": {},
    //Test
    "name": "${NAME}",
    "platform": "wgs",
    "config": "${DIR}/config.json",
    "fam": "${DIR}/${NAME}.fam",
    "patient-ids": "${DIR}/${NAME}.csv",
    "vcf": "${DIR}/${NAME}.vcf",
    "vep-json": "${DIR}/${NAME}_vep.json",
    "a-json": "${DIR}/${NAME}_anfisa.json.gz",
    "docs": []    
}

